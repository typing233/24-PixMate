[project]
name = "pixmate"
version = "0.1.0"
description = "Terminal pixel art companion for Claude Code sessions"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
pixmate = "pixmate.__main__:main"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"
